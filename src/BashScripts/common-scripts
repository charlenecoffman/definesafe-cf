#!/bin/bash

function create_matrix_output {
    echo $1>&2
    arr=("$@")
    matrixOutput="files={\"include\":["
    for i in "${arr[@]}"
    do
        matrixOutput+="$i,"
    done
    matrixOut="${matrixOutput%,}]}"
    echo $matrixOut>&2
}